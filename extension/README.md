# Chrome Extension（实验版）

这是 Mnet Plus Subtitle Downloader 的 Manifest V3 Chrome 扩展实验版。

## 安装

1. 下载或 clone 整个仓库。
2. Chrome 打开 `chrome://extensions/`。
3. 开启右上角「开发者模式」。
4. 点击「加载已解压的扩展程序」。
5. 选择本仓库的 `extension/` 文件夹。
6. 建议把扩展固定到浏览器工具栏。

更新仓库文件后，如扩展已经加载，请在 `chrome://extensions/` 中点击该扩展的「重新加载」按钮。

## 使用

1. 正常登录 Mnet Plus。
2. 打开一个 Mnet Plus 视频页面。
3. 点击工具栏里的 **Mnet Plus Subtitle Downloader**。
4. 扩展会读取当前视频真实提供的字幕轨。
5. 所有字幕默认均不勾选；请自行选择需要的语言。
6. 点击「生成选中字幕」。扩展会先抓取并转换为 SRT，但不会立刻弹出系统保存窗口。
7. 生成完成后，下方会出现「已准备好的字幕」资源列表。
8. 对需要保存的文件逐个点击「保存」。

## 关于 Chrome 的“每次下载前询问保存位置”

扩展兼容该浏览器设置，不要求关闭它。

字幕会先暂存在当前 Mnet Plus 页面内存中。因此：

- 系统“另存为”窗口里点击「取消」，不会丢失已经生成的字幕。
- 重新打开扩展后，已准备好的字幕仍会显示，可以再次点击「保存」。
- 不需要重新抓取整段视频字幕。

缓存只保留在当前视频页面生命周期内。如果刷新页面、关闭标签页或导航到其他页面，已准备好的资源会被清除，需要重新生成。

## 字幕轨

扩展不会写死语言。它根据视频元数据中的 `languageConfigs` 动态显示当前视频实际提供的字幕，例如：

- `ko` 韩国语
- `en` 英语
- `ja` 日本语
- `zh_CN` 中文简体
- `zh_TW` 中文繁体

如果 Mnet Plus 给某个轨道返回 AI 自动生成标记，扩展会一并显示。

## 当前状态

**Experimental / 实验版。**

目前已经完成：

- Manifest V3 配置
- Mnet Plus 视频页 content script
- 浏览器工具栏 popup
- 自动识别字幕轨
- 字幕轨默认不勾选
- 多选生成字幕
- 下载进度显示
- SRT 导出
- 已生成资源缓存
- 单文件重复保存 / 取消后重试

仍建议继续测试：

- 超长视频的抓取稳定性
- Mnet Plus 客户端路由切换后的缓存清理行为
- 不同账号 / 地区 / 字幕配置的视频兼容性

## 安全说明

扩展只在匹配的 Mnet Plus 视频页面工作，并使用当前浏览器中已经存在的正常登录状态请求字幕。请勿将 Cookie、Authorization token 或 HAR 文件公开分享。

本项目仅供个人学习、语言学习、字幕研究、可访问性研究和技术学习。详见仓库根目录 README。
